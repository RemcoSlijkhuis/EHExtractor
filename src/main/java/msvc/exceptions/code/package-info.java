/**
 * Contains helper classes for finding function prologues matching how Microsoft Visual C++ (MSVC) creates these when exception handling (EH) is in play,
 * and for matching code that registers the MSVC EH entry data structure (with optional security cookie-checking code before that).
 */
package msvc.exceptions.code;